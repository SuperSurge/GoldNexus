<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!-- 统一继承 Spring Boot 父工程，管理所有 Spring Boot 相关依赖版本 -->
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.9</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>com.goldnexus</groupId>
	<artifactId>goldnexus-cloud</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>goldnexus-cloud</name>
	<description>金纽贷 —— Spring Cloud 微服务聚合父工程</description>
	<packaging>pom</packaging>

	<!-- 聚合的子模块列表 -->
	<modules>
		<module>common</module>
		<module>gateway</module>
		<module>user-service</module>
		<module>loan-service</module>
		<module>content-service</module>
	</modules>

	<properties>
		<java.version>21</java.version>
		<!-- Spring Cloud 版本（对应 Spring Boot 3.5.x） -->
		<spring-cloud.version>2025.0.0</spring-cloud.version>
		<!-- Spring Cloud Alibaba 版本（内置 Nacos 客户端） -->
		<spring-cloud-alibaba.version>2025.0.0.0</spring-cloud-alibaba.version>
	</properties>

	<!--
		通过 BOM（物料清单）统一管理 Spring Cloud / Alibaba 各组件的版本，
		子模块引用相关依赖时无需再写版本号，避免版本冲突。
	-->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.alibaba.cloud</groupId>
				<artifactId>spring-cloud-alibaba-dependencies</artifactId>
				<version>${spring-cloud-alibaba.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<!-- 打包可执行 jar 时排除 Lombok（编译期注解，运行时不需要） -->
				<plugin>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-maven-plugin</artifactId>
					<configuration>
						<excludes>
							<exclude>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
							</exclude>
						</excludes>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

</project>
